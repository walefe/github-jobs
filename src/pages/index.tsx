import { FC } from 'react';

import Header from '@/components/Header';
import Layout from '@/components/Layout';

const Home: FC = () => (
  <Layout>
    <Header />
  </Layout>
);

export default Home;
